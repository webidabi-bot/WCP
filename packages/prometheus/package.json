{
  "name": "@aios/prometheus",
  "version": "0.1.0",
  "description": "Persistence, backup, and metrics service for AI-Stack / AIOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test dist/**/*.test.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12"
  }
}
