/**
 * @aios/phoenix
 *
 * Control-plane API server for AI-Stack / AIOS.
 *
 * Exports the createApp factory and the App class for embedding in tests
 * or other entry points.
 */

export { createApp, App } from "./app.js";
export type { AppOptions } from "./app.js";
