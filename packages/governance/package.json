{
  "name": "@aios/governance",
  "version": "0.1.0",
  "description": "PostgreSQL-backed agent governance layer for AI-Stack / AIOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "migrate": "node dist/migrate.js",
    "migrate:rollback": "node dist/migrate.js rollback"
  },
  "dependencies": {
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12"
  }
}
