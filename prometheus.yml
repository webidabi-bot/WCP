# Prometheus scrape configuration for AI-Stack / AIOS
global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    service: aios

scrape_configs:
  - job_name: "phoenix"
    static_configs:
      - targets: ["phoenix:3000"]
    metrics_path: /metrics
    scrape_interval: 15s
